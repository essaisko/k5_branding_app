rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계 - 임시로 모든 접근 허용
    // 추후 프로덕션에서는 적절한 권한 설정 필요
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 